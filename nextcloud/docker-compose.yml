# nextcloud - docker compose (single host)
# image verified to exist at generation time: nextcloud:stable

services:
  nextcloud:
    image: nextcloud:stable
    container_name: nextcloud
    restart: unless-stopped
    ports:
      - "20165:80"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:80/ || wget -qO- http://127.0.0.1:80/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=nextcloud"
    #   - "homepage.description=Suite of client-server software for file syncing, collaboration, and video conferencing."""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=nextcloud.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20165"
    #   - "homepage.server=localhost"

networks:
  default:
    name: nextcloud_net
