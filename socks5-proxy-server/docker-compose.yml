# socks5-proxy-server - docker compose (single host)
# image verified to exist at generation time: serjs/go-socks5-proxy:latest

services:
  socks5-proxy-server:
    image: serjs/go-socks5-proxy:latest
    container_name: socks5-proxy-server
    restart: unless-stopped
    ports:
      - "1080:1080"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 1080 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=socks5-proxy-server"
    #   - "homepage.description=Self-hosted SOCKS5 proxy server.""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=socks5-proxy-server.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:1080"
    #   - "homepage.server=localhost"

networks:
  default:
    name: socks5-proxy-server_net
