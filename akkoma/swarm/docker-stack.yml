# akkoma - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml akkoma
# image verified to exist at generation time: akkoma/akkoma:latest

services:
  akkoma:
    image: akkoma/akkoma:latest
    ports:
      - target: 20400
        published: 20400
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - akkoma_config:/config
      - akkoma_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20400 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=akkoma"
    #   - "homepage.description=Lightweight federated microblogging server (ActivityPub, Mastodon-compatible).""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=akkoma.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20400"
    #   - "homepage.server=localhost"

volumes:
  akkoma_config:
  akkoma_data:

networks:
  default:
    name: akkoma_net
    driver: overlay
    attachable: true
