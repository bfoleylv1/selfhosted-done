# proxmox - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml proxmox
# image verified to exist at generation time: ubuntu:24.04

services:
  proxmox:
    image: ubuntu:24.04
    ports:
      - target: 8006
        published: 20222
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      # - LIBVA_DRIVER_NAME=iHD
      # - INTEL_GPU=1
    volumes:
      - proxmox_config:/config
      - proxmox_data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8006/ || wget -qO- http://127.0.0.1:8006/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
          # - node.role == manager
          # GPU: add node-generic-resources to daemon.json and:
          #   - node.labels.gpu == true
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
      # GPU: swarm ignores `devices:`/`runtime:`. Declare in
      #   /etc/docker/daemon.json: "node-generic-resources": ["gpu=1"]
      #   then use deploy.resources.reservations.generic_resources.
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=proxmox"
    #   - "homepage.description=Open-source server virtualization platform (Proxmox VE).""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=proxmox.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20222"
    #   - "homepage.server=localhost"

volumes:
  proxmox_config:
  proxmox_data:

networks:
  default:
    name: proxmox_net
    driver: overlay
    attachable: true
