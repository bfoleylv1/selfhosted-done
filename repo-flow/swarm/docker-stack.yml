# repo-flow - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml repo-flow
# image verified to exist at generation time: alpine:3.20

services:
  repo-flow:
    image: alpine:3.20
    ports:
      - target: 20240
        published: 20240
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - repo-flow_config:/config
      - repo-flow_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20240 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=repo-flow"
    #   - "homepage.description=Placeholder entry — no real upstream image configured (generic base image only); not yet implemented."""""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=repo-flow.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20240"
    #   - "homepage.server=localhost"

volumes:
  repo-flow_config:
  repo-flow_data:

networks:
  default:
    name: repo-flow_net
    driver: overlay
    attachable: true
