# rclone - docker compose (single host)
# image verified to exist at generation time: rclone/rclone:latest

services:
  rclone:
    image: rclone/rclone:latest
    container_name: rclone
    restart: unless-stopped
    ports:
      - "5572:5572"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 5572 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=rclone"
    #   - "homepage.description=rclone self-hosted service."
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=rclone.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:5572"
    #   - "homepage.server=localhost"

networks:
  default:
    name: rclone_net
