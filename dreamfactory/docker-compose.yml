# dreamfactory - docker compose (single host)
# image verified to exist at generation time: dreamfactorysoftware/df-docker:latest

services:
  dreamfactory:
    image: dreamfactorysoftware/df-docker:latest
    container_name: dreamfactory
    restart: unless-stopped
    ports:
      - "20051:20051"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20051 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=dreamfactory"
    #   - "homepage.description=Auto-generated REST API backend for databases.""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=dreamfactory.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20051"
    #   - "homepage.server=localhost"

networks:
  default:
    name: dreamfactory_net
