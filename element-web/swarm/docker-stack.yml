# element-web - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml element-web
# image verified to exist at generation time: vectorim/element-web:latest

services:
  element-web:
    image: vectorim/element-web:latest
    ports:
      - target: 20058
        published: 20058
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - element-web_config:/config
      - element-web_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20058 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=element-web"
    #   - "homepage.description=Web client for the Matrix decentralized chat protocol."""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=element-web.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20058"
    #   - "homepage.server=localhost"

volumes:
  element-web_config:
  element-web_data:

networks:
  default:
    name: element-web_net
    driver: overlay
    attachable: true
