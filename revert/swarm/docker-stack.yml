# revert - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml revert
# image verified to exist at generation time: node:20-alpine

services:
  revert:
    image: node:20-alpine
    ports:
      - target: 20247
        published: 20247
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - revert_config:/config
      - revert_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20247 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=revert"
    #   - "homepage.description=Self-hosted service.""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=revert.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20247"
    #   - "homepage.server=localhost"

volumes:
  revert_config:
  revert_data:

networks:
  default:
    name: revert_net
    driver: overlay
    attachable: true
