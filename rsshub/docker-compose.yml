# rsshub - docker compose (single host)
# image verified to exist at generation time: diygod/rsshub:latest

services:
  rsshub:
    image: diygod/rsshub:latest
    container_name: rsshub
    restart: unless-stopped
    ports:
      - "1200:1200"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:1200/ || wget -qO- http://127.0.0.1:1200/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=rsshub"
    #   - "homepage.description=Everything is an RSS feed; aggregates content from various sources to RSS.""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=rsshub.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:1200"
    #   - "homepage.server=localhost"

networks:
  default:
    name: rsshub_net
