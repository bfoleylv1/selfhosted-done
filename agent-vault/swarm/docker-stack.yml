# agent-vault - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml agent-vault
# image verified to exist at generation time: hashicorp/vault:latest

services:
  agent-vault:
    image: hashicorp/vault:latest
    ports:
      - target: 20398
        published: 20398
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - agent-vault_config:/config
      - agent-vault_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20398 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=agent-vault"
    #   - "homepage.description=agent-vault self-hosted service."
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=agent-vault.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20398"
    #   - "homepage.server=localhost"

volumes:
  agent-vault_config:
  agent-vault_data:

networks:
  default:
    name: agent-vault_net
    driver: overlay
    attachable: true
