# gpt-engineer - docker compose (single host)
# image verified to exist at generation time: python:3.11-slim

services:
  gpt-engineer:
    image: python:3.11-slim
    container_name: gpt-engineer
    restart: unless-stopped
    ports:
      - "20102:80"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      # - NVIDIA_DRIVER_CAPABILITIES=all
      # - NVIDIA_VISIBLE_DEVICES=all
      # - ONEAPI_DEVICE_SELECTOR=*        # Intel iGPU/Arc OpenVINO
      # - ZES_ENABLE_SYSMAN=1
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:80/ || wget -qO- http://127.0.0.1:80/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # devices:
    #   - /dev/dri:/dev/dri
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=gpt-engineer"
    #   - "homepage.description=AI-powered code generation tool; create projects from descriptions.""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=gpt-engineer.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20102"
    #   - "homepage.server=localhost"

networks:
  default:
    name: gpt-engineer_net
