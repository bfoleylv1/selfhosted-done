# instapaper - docker compose (single host)
# image verified to exist at generation time: alpine:3.20

services:
  instapaper:
    image: alpine:3.20
    container_name: instapaper
    restart: unless-stopped
    ports:
      - "20116:20116"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20116 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=instapaper"
    #   - "homepage.description=Web article saver; read later and save content."""""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=instapaper.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20116"
    #   - "homepage.server=localhost"

networks:
  default:
    name: instapaper_net
