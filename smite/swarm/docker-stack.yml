# smite - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml smite
# image verified to exist at generation time: alpine:3.20

services:
  smite:
    image: alpine:3.20
    ports:
      - target: 20285
        published: 20285
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - smite_config:/config
      - smite_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20285 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=smite"
    #   - "homepage.description=Self-hosted service."""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=smite.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20285"
    #   - "homepage.server=localhost"

volumes:
  smite_config:
  smite_data:

networks:
  default:
    name: smite_net
    driver: overlay
    attachable: true
