# 42links - docker compose (single host)
# image verified to exist at generation time: node:20-alpine

services:
  42links:
    image: node:20-alpine
    container_name: 42links
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 8080 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=42links"
    #   - "homepage.description=42links self-hosted service."
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=42links.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:8080"
    #   - "homepage.server=localhost"

networks:
  default:
    name: 42links_net
