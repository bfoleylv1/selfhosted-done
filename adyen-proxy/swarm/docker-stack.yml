# adyen-proxy - docker swarm stack
# deploy: docker stack deploy -c docker-stack.yml adyen-proxy
# image verified to exist at generation time: node:20-alpine

services:
  adyen-proxy:
    image: node:20-alpine
    ports:
      - target: 3000
        published: 20403
        protocol: tcp
        mode: ingress
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - adyen-proxy_config:/config
      - adyen-proxy_data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 3000 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
    # labels:
    #   - "homepage.group=Self Hosting Solutions"
    #   - "homepage.name=adyen-proxy"
    #   - "homepage.description=adyen-proxy self-hosted service."
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=adyen-proxy.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20403"
    #   - "homepage.server=localhost"

volumes:
  adyen-proxy_config:
  adyen-proxy_data:

networks:
  default:
    name: adyen-proxy_net
    driver: overlay
    attachable: true
