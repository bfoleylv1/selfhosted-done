# adyen-proxy - docker compose (single host)
# image verified to exist at generation time: node:20-alpine

services:
  adyen-proxy:
    image: node:20-alpine
    container_name: adyen-proxy
    restart: unless-stopped
    ports:
      - "20396:20396"
    environment:
      - TZ=${TZ:-America/Los_Angeles}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ./config:/config
      - ./data:/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 20396 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # labels:
    #   - "homepage.group=Self Hosted"
    #   - "homepage.name=adyen-proxy"
    #   - "homepage.description=Reverse proxy / bridge for the Adyen payment API."""
    #   - "homepage.tags=Self Hosting Solutions"
    #   - "homepage.icon=adyen-proxy.png"
    #   - "homepage.href=http://${HOST_IP:-localhost}:20396"
    #   - "homepage.server=localhost"

networks:
  default:
    name: adyen-proxy_net
