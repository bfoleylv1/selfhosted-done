services:
  piqueserver:
    image: ghcr.io/piqueserver/piqueserver:latest
    ports:
    - target: 20495
      published: 20495
      protocol: tcp
      mode: ingress
    environment:
    - TZ=${TZ:-America/Los_Angeles}
    - PUID=${PUID:-1000}
    - PGID=${PGID:-1000}
    volumes:
    - piqueserver_config:/config
    - piqueserver_data:/data
    healthcheck:
      test:
      - CMD-SHELL
      - nc -z 127.0.0.1 20495 || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
        - node.platform.os == linux
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        order: start-first
        failure_action: rollback
      resources:
        limits:
          memory: 2G
volumes:
  piqueserver_config: null
  piqueserver_data: null
networks:
  default:
    name: piqueserver_net
    driver: overlay
    attachable: true
